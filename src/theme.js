import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#186569",
      light: "#186569",
      first: "#186569",
    },
    secondary: {
      main: "#15c630",
    },
    otherColor: {
      main: "#999",
    },
  },
});

export default theme;
